import '../styles/PokemonEvolution.css';

export default function PokemonEvolution() {
      return (
        <section className="evolution-container">
            {/* <img
                src={oldPokemon.image}
                alt={oldPokemon.name}
                className='oldPokemon'
            />
            <img
                src={newPokemon.image}
                alt={newPokemon.name}
                className='newPokemon'
            />
            <p>What? {oldPokemon.name} is evolving!</p>
            <p>{oldPokemon.name} evolved into {newPokemon.name}!</p> */}
        </section>
    );
}